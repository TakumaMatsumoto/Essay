\chapter{本研究に関連する従来研究} 
\label{chap:theories}

%-----------------------------------------------------

\section{非線形光学効果}
\subsection{非線形光学効果の概要}
レーザー出現以前に知られていた弱い光によっておこる直進，反射，屈折，回折，干渉，吸収，発光，散乱といった様々な光学現象はすべて線形光学現象と呼ばれる．ここで線形であることは光に対する物質の応答が線形すなわち光電場に比例しているということを意味している．これは，光の電界によって誘起される分極によるものである．光の強度が小さい範囲では物質の分極が光の電界の大きさに比例するが，光の強度が大きくなると分極が光の電界に比例しなくなり，非線形性が見られるようになる．このとき発生する現象が非線形光学現象である{\cite{non_linear}}．

\subsection{非線形屈折率変化と非線形現象}
非線形屈折率変化とは光の強度に応じて屈折率が変化する現象で，分極の非線形性が原因となって引き起こされる．光強度を$I$，線形な範囲での屈折率を$n_0$，非線形屈折率係数を$n_2$とすると屈折率は，
\begin{equation}
  n = n_{0} + n_{2}I
\end{equation}
と表すことができる．

非線形屈折率変化によって引き起こされる現象には，自分自身の光の強度により位相が変化する自己位相変調(SPM:Self Phase Modulation)，他の光の強度により位相が変化する相互位相変調(XPM:Cross Phase Modulation)が存在する．

\newpage

\section{半導体光増幅器(SOA:Semiconductor Optical Amplifier)}
SOAは半導体のエネルギーバンドギャップを利用した光増幅器である{\cite{optic}}．SOA内のキャリアが励起している状態でSOAに光を入力すると位相と周波数が等しい光がキャリアから放出され，活性層内で光が増幅される．

\subsection{SOAの基本構造}
図{\ref{fig:model_of_soa}}にSOAの簡易構造を示す．SOAはp型とn型の異なる原子配列を持った半導体に活性層が挟み込まれた構造となっており，この活性層で光が増幅される．活性層には単一の元素からなる，不純物を含まない真性半導体が用いられるが，p型とn型の半導体には，キャリア（自由電子や正孔のように電荷を運ぶもの）を増やし電気伝導率を上げるために真性半導体に不純物を添加した不純物半導体を用いる．このうち，自由電子キャリアを増加させたものをn型半導体，自由正孔キャリアを増加させたものをp型半導体と言う．

\begin{figure}[htbp]
  \begin{center}
    \includegraphics[clip,width=12.0cm]{model_of_soa.eps}
    \caption{SOAの簡易構造}
    \label{fig:model_of_soa}
  \end{center}
\end{figure}

\newpage

\subsection{n型半導体，p型半導体}
図{\ref{fig:n_semiconductor}}，図{\ref{fig:p_semiconductor}}では最外殻の電子だけを示している．真性半導体のシリコンは価電子が4個の原子であり，図{\ref{fig:n_semiconductor}}のように価電子が5個のリンを微量だけ添加するとリンはシリコンの間に入り，最外殻に価電子を8個持つ共有結合を形成する．このとき電子が1つ余り自由電子となって電気伝導に寄与する．これをn型半導体という．一方，図{\ref{fig:p_semiconductor}}のようにシリコンに価電子が3個のボロンを微量だけ添加するとボロンはシリコンの間に入り，最外殻に価電子を8個持つ共有結合を形成する．このとき電子が1つ欠損し，この欠損している電子があるべき場所をホールという．このホールに近くの電子が飛び込むことで，あたかも正電荷を有する電子が動くように電気伝導に寄与する．これをp型半導体という．

\begin{figure}[htbp]
  \begin{center}
    \includegraphics[clip,width=8.0cm]{n_semiconductor.eps}
    \caption{N型半導体のモデル図}
    \label{fig:n_semiconductor}
    \includegraphics[clip,width=8.0cm]{p_semiconductor.eps}
    \caption{P型半導体のモデル図}
    \label{fig:p_semiconductor}
  \end{center}
\end{figure}

\subsection{SOAにおける光の増幅原理}
二つのエネルギー準位$E_{1}，E_{2}(E_{1}<E_{2})$をもつ原子系を考える．一般に高いエネルギー準位（準位2）にあるキャリアは，ある確率で低いエネルギー準位（準位1）に遷移する．この過程を図(左)に示す．この過程は媒質に入射する光に左右されないため，自然放出と呼ばれる．\\
一方，媒質に光が入射すると，準位1にあるキャリアが準位2に遷移する誘導吸収と，準位2にあるキャリアが準位1に遷移する誘導放出が起こる．これは媒質に入射する光によって起こるため，誘導遷移過程と呼ばれる．誘導吸収過程を図(中央)に，誘導放出過程を図(右)に示す．\\
ここで，準位1におけるキャリアの数を$N_{1}$，準位2におけるキャリアの数を$N_{2}$とすると，平衡状態では$N_{1}>N_{2}$となる．SOAにおいては，電流によってキャリアを注入することによって，反転分布$N_{1}<N_{2}$を形成する．反転分布においては，誘導吸収されるキャリアの数より，誘導放出されるキャリアの数を大きくなるため，光が増幅される．

\begin{figure}[htbp]
  \begin{center}
    \includegraphics[clip,width=14.0cm]{amp_phenomenon.eps}
    \caption{キャリアの遷移過程}
    \label{fig:amp_phenomenon}
  \end{center}
\end{figure}

\newpage

% TODO::GaInAsPとInGaAsの参考文献がほしい
\section{量子井戸(QW:Quantum Well)}
QWとは2種類の半導体薄膜が交互に積み重なった構造のことであり，キャリアを2次元方向に閉じ込めることができる．図{\ref{fig:qw_energy}}にQWのエネルギーバンド図を示す．QWでは伝導帯・価電子帯に階段状のエネルギー準位が形成される．これに伴いスペクトル幅が狭くなり，光学利得が改善される．Bulk部にはQW部よりもエネルギーバンドギャップが大きいガリウムひ素(GaAs)が用いられる．QW部に用いられる材質は使用する波長帯域によって異なり，1550nm付近の波長の光を増幅するにはひ化りん化インジウムガリウム(GaInAsP)が用いられ，1300nm付近の波長の光を増幅するにはインジウムガリウムヒ化物(InGaAs)が用いられる．量子井戸構造はQW部がBulk部に挟まれる構造であり，キャリアはバンドギャップの小さいQW部に集中する．エネルギー準位の遷移はエネルギーバンドギャップの小さい材質の伝導帯，価電子帯の準位間で生じるため吸収，発光の効率を高くすることができる．

\begin{figure}[htbp]
  \begin{center}
    \includegraphics[clip,width=14.0cm]{qw_energy.eps}
    \caption{QWのバンド図}
    \label{fig:qw_energy}
  \end{center}
\end{figure}

\newpage

\section{量子ドット(QD:Quantum Dot)}
QDは複数の半導体原子から構成されており，キャリアを3次元方向に閉じ込めることができる．図{\ref{fig:qd_energy}}にQDのエネルギーバンド図を示す．QDはQW以上に小さいバンドギャップを形成するためQWよりも吸収，発光の効率を高くすることができる．QDはStranski-Krastanovモードと呼ばれる手法で形成する方法が一般的であり，多くの材料について行われてきている{\cite{qd_book}}．これはGaAsの基盤の上にIn原子とAs原子をぶつけてGaAs基板上に薄い膜を生成し、膜が厚くなるとInAsが自然と凝集してQDが形成されるという現象を利用している．

\begin{figure}[htbp]
  \begin{center}
    \includegraphics[clip,width=14.0cm]{qd_energy.eps}
    \caption{QDのバンド図}
    \label{fig:qd_energy}
  \end{center}
\end{figure}

\newpage

\section{量子ドット半導体光増幅器(QD-SOA)}
QD-SOAとは活性層が量子ドット構造で構成される半導体光増幅器である．図{\ref{fig:model_of_qd_soa}}にQD-SOAのモデル図を示す．QD-SOAはSOAと比較して増幅に必要な電流値が小さい，利得の回復が早い，温度に対する依存性が低いなどの特性をもつ光増幅器であり，現在最も増幅の特性がよいとされている{\cite{qd_soa}}．これらの特性が生まれる理由は増幅に関わる電子が量子ドットに局所的に集まることで，電子のエネルギー準位が離散化され，注入されたキャリアの大部分が光の増幅に寄与するためである．

\begin{figure}[htbp]
  \begin{center}
    \includegraphics[clip,width=14.0cm]{model_of_qd_soa.eps}
    \caption{QD-SOAのモデル図}
    \label{fig:model_of_qd_soa}
  \end{center}
\end{figure}

\newpage

\subsection{QD-SOAのエネルギーバンド構造}
図{\ref{fig:qd_soa_energy}}にQD-SOAのエネルギー構造のモデル図を示す{\cite{qd_soa_rate_equation}}．QD-SOAにおけるエネルギー準位はQW(Quantum Well)，US(Upper State)，ES(Excited State)，GS(Ground State)の4準位でモデル化される．図中の矢印はキャリアのエネルギー遷移を示しており，伝導帯・価電子帯のキャリアはともに(QW，US)間，(US，ES)間，(US，GS)間，(ES，GS)間のみで遷移することを意味している．電流により伝導帯のQWに注入されたキャリアは，US，ES，GSへと遷移し，伝導帯のGSから価電子帯のGSへとエネルギー準位が下がる際の誘導放出により光は増幅される．

\begin{figure}[htbp]
  \begin{center}
    \includegraphics[clip,width=12.0cm]{qd_soa_energy.eps}
    \caption{量子ドットのエネルギー構造のモデル図}
    \label{fig:qd_soa_energy}
  \end{center}
\end{figure}

\section{QD-SOAの入出力特性の解析}
ここでは，QD-SOAの入出力特性の解析に用いる手法，式について説明する．以降で用いる文字とその意味を表{\ref{table:qd_soa_param}}に示す．

\renewcommand{\arraystretch}{1.5}
\begin{center}
\begin{longtable}[c]{|c|c|}
 \caption{解析の式に用いる文字，意味\label{table:qd_soa_param}} \\ \hline
  文字 & 意味 \\ \hline \hline \endfirsthead
  $L_{ca}$  			& 活性層の長さ			 \\ \hline
  $W$        			& 活性層の幅 	 		 \\ \hline
  $L_{QW}$ 			& 量子井戸の高さ	 		 \\ \hline
  $L_{H}$ 				& 量子ドットの高さ	 		 \\ \hline
  $l_{D}$ 				& 量子ドット層の数	 		 \\ \hline 
  $T$ 				& 室温 			 		 \\ \hline
  $n_{D}$ 				& 量子ドット密度 	 		 \\ \hline
  $N_{QD}$ 			& 量子ドットの数 			 \\ \hline
  $D^{c,v}_{g}$ 			& 縮退係数(GS層) 		 \\ \hline
  $D^{c,v}_{e}$ 		& 縮退係数(ES層) 		 \\ \hline
  $D^{c(v)}_{u}$ 		& 縮退係数(US層) 		 \\ \hline
  $D^{c(v)}_{w}$ 		& 縮退係数(QW層) 		 \\ \hline
  $|M_{env}|^2$ 		& 包絡関数 				 \\ \hline
  $\langle|\hat{e} \cdot p_{cv}|^2\rangle$ & 運動量行列 	 \\ \hline
  $f_{D}$ 				& ドープ密度 		 		\\ \hline
  $m^{*c(v)}_{w}$ 		& キャリアの有効質量(QW層) \\ \hline
  $m^{*c(v)}_{d}$ 		& キャリアの有効質量(QD層) \\ \hline
  $\Gamma^{c(v)}_{ih}$ 	& 不均一広がりの半値全幅		\\ \hline
  $\Gamma_{ih}$ 		& 不均一広がりの半値全幅の和	\\ \hline
  $\Gamma_{h}$ 		& 均一広がりの半値全幅		\\ \hline
  $\Gamma$ 			& 閉じ込め係数(QD層)			\\ \hline
  $\Gamma_{w}$ 		& 閉じ込め係数(QW層)			\\ \hline
  $\hat{\tau}^{c(v)}_{eg}$ & キャリア緩和時間(ES$\to$GS) \\ \hline
  $\hat{\tau}^{c(v)}_{ge}$ & キャリア脱出時間(GS$\to$ES) \\ \hline 
  $\hat{\tau}^{c(v)}_{ug}$ & キャリア緩和時間(US$\to$GS) \\ \hline
  $\hat{\tau}^{c(v)}_{gu}$ & キャリア脱出時間(GS$\to$US) \\ \hline
  $\hat{\tau}^{c(v)}_{ue}$ & キャリア緩和時間(US$\to$ES) \\ \hline 
  $\hat{\tau}^{c(v)}_{eu}$ & キャリア脱出時間(ES$\to$US) \\ \hline 
  $\hat{\tau}^{c(v)}_{wu}$ & キャリア緩和時間(QW$\to$US) \\ \hline
  $\hat{\tau}^{c(v)}_{uw}$ & キャリア脱出時間(US$\to$QW) \\ \hline
  $\hat{\tau}_{dr}$ 		  & 自然放出時間 				 \\ \hline
  $\hat{\tau}^{c(v)}_{wr}$ & 自然放出時間(QW層)		 \\ \hline
  $\hat{\tau}^{c(v)}_{ur}$ & 自然放出時間(US層) 		 \\ \hline
  $r_{c(v)}$ 	 		& サンプル間隔の係数		 \\ \hline
  $\Delta E$ 			& サンプル間隔 			 \\ \hline
  $2M+1$ 	 		& サンプル数 				 \\ \hline
  $E^{c(v)}_{g(e,u),j}$ 	& エネルギー準位(j番目QD層) \\ \hline
  $n_r$ 	 	 		& QD層の物質屈折率 		 \\ \hline
  $V_{d}$ 				& QD層の体積 			 \\ \hline
  $V_{w}$ 	 		& QW層の体積 			 \\ \hline
  $I$ 		 		& 注入電流  				 \\ \hline
  $N^{c(v)}_{W}$  & 最大許容キャリア数(QW層)		 \\ \hline
  $N^{c(v)}_{U,j}$ & 最大許容キャリア数(j番目US層) 	 \\ \hline
  $N^{c(v)}_{E,j}$ & 最大許容キャリア数(j番目ES層) 	 \\ \hline
  $N^{c(v)}_{G,j}$ & 最大許容キャリア数(j番目GS層) 	 \\ \hline
  $G^{c(v)}_{j}$   & 不均一広がりのうちj番目の割合 	 \\ \hline
  $\varepsilon_0$		& 真空中における誘電率 	 \\ \hline
  $\mu_0$			& 真空中における透磁率 	 \\ \hline
  $m_0$		 & 電子の質量 	 \\ \hline
  $q$ 		 & 電荷素量 		 \\ \hline
  $k_{B}$ 		 & ボルツマン定数  	 \\ \hline
  $\hbar$ 	 & ディラック定数 	 \\ \hline
  $c$ 		 & 光速 			 \\ \hline
  $\alpha_{i}$ 	 & 損失係数 		 \\ \hline
  $P_{k}$ 	& 波長kの光強度 		 \\ \hline
  $E_{k}$ 	& 波長kの光エネルギー 	 \\ \hline
\end{longtable}
\end{center}
\renewcommand{\arraystretch}{1.0}

\newpage

\subsection{QD各層のエネルギー準位，存在確率}
QDはどのような製造過程においても大きさを制御することができないため，エネルギー準位にばらつきが生じる{\cite{qd_dispersion}}．各層のエネルギー準位の存在確率は正規分布に従うものとし，サンプル数を$2M+1$として量子化を行うと伝導帯，価電子帯のj番目のエネルギー準位は最も密度が高い標本番号を$M$とするとそれぞれ
\begin{eqnarray}
	E^{c}_{u(e,g),j} & = & E^{c}_{u(e,g),M} + \left[ j - \left( M+1 \right) \right] \times r_{c} \Delta E \\
	E^{v}_{u(e,g),j} & = & E^{v}_{u(e,g),M} - \left[ j - \left( M+1 \right) \right] \times r_{v} \Delta E
\end{eqnarray}
と表される．図{\ref{fig:energy_dist}}にQD各層のエネルギー準位と密度の関係を示す．図{\ref{fig:energy_dist}}のように各層のエネルギー準位の密度は正規分布に従うものとし，このときj番目の割合は平均を$E^{c(v)}_{g,M}$分散を$\sigma_{in,c(v)} = \Gamma^{c(v)}_{ih} / 2.356$とした正規分布となり，
\begin{equation}
  G^{c(v)}_{j} = \frac{1}{ \sqrt{2 \pi \sigma^{2}_{in,c(v)}}} \exp\left[-\frac{ \left( E^{c(v)}_{g,j} - E^{c(v)}_{g,max} \right)^{2} }{ 2 \sigma^{2}_{in,c(v)} } \right] \times r_{c(v)} \Delta E
\end{equation}
と表すことができる．

\begin{figure}[htbp]
  \begin{center}
    \includegraphics[clip,width=12.0cm]{energy_dist.eps}
    \caption{エネルギー準位と密度の関係}
    \label{fig:energy_dist}
  \end{center}
\end{figure}

\newpage

\subsection{キャリア遷移時間，脱出時間}
キャリアの遷移時間はポンプ，プローブ法による実験から求めることができる．また，キャリアの脱出時間は釣り合いの原理を用いてキャリアの遷移時間，及び縮退係数から求めることができる．伝導帯におけるy層からx層へのキャリア脱出時間は({\ref{eq:cb_carrier_escape_time}})式より求めることができる{\cite{qd_dispersion}}．
\begin{equation}
  \label{eq:cb_carrier_escape_time}
  \tau^{c(v)}_{yx} = 
	f^{c(v)}_{D} \tau^{c(v)}_{xy} 
	\frac{D_{y}}{D_{x}} 
	\exp\left(\frac
	{\Delta E^{c(v)}_{xy}}
	{k_{B}T}\right)
\end{equation}


\subsection{QD-SOAのレート方程式}
QD-SOAの入出力特性を解析するためには，QD-SOAに光を入射した際のキャリアの増減に起因する利得の変化，およびそれに伴う光の振幅と位相の変化を計算する必要がある．半導体発光デバイスにおけるキャリアの時間変化はレート方程式と呼ばれる微分方程式で記述できることが知られている{\cite{qd_soa_rate_equation}}．本研究に用いるQD-SOAのレート方程式を({\ref{eq:qd_qw}})式から({\ref{eq:qd_gs}})式に示す．

\begin{eqnarray}
  \label{eq:qd_qw}
  \frac{ {\partial}f^{c(v)}_{w} }{ {\partial}t } & = & 
	\frac{I}{qN^{c(v)}_{W}} - 
	\frac{ \sqrt{ f^{c}_{w} f^{v}_{w} } }{ \hat{\tau}^{c(v)}_{wr} } + 
	\frac{ D^{c(v)}_{u} }{ D^{c(v)}_{w} } \sum_{j=1}^{2M+1} G^{c(v)}_{j} \nonumber\\ &&
	\times
	\left(
		\frac{ f^{c(v)}_{u,j} }{ \hat{\tau}^{c(v)}_{uw,j} }	\left(1 - f^{c(v)}_{w} \right)
		\frac{ f^{c(v)}_{w}  }{ \hat{\tau}^{c(v)}_{wu}  } 	\left(1 - f^{c(v)}_{u,j}\right)
	\right)
  \\
  \label{eq:qd_us}
  \frac{ {\partial}f^{c(v)}_{u,j} }{ {\partial}t } & = & 
	\frac{ f^{c(v)}_{w}  }{ \hat{\tau}^{c(v)}_{wu}  }	\left(1 - f^{c(v)}_{u,j}\right) -
	\frac{ f^{c(v)}_{u,j} }{ \hat{\tau}^{c(v)}_{uw,j} }	\left(1 - f^{c(v)}_{w} \right) \nonumber\\ &&
	+ \frac{ D^{c(v)}_{e} }{ D^{c(v)}_{u} } \left(
		\frac{ f^{c(v)}_{e,j} }{ \hat{\tau}^{c(v)}_{eu} } \left(1 - f^{c(v)}_{u,j}\right)
		\frac{ f^{c(v)}_{u,j} }{ \hat{\tau}^{c(v)}_{ue} } \left(1 - f^{c(v)}_{e,j}\right)
	\right)  \nonumber\\ &&
	+ \frac{ D^{c(v)}_{g} }{ D^{c(v)}_{u} } \left(
		\frac{ f^{c(v)}_{g,j} }{ \hat{\tau}^{c(v)}_{gu} } \left(1 - f^{c(v)}_{u,j}\right)
		\frac{ f^{c(v)}_{u,j} }{ \hat{\tau}^{c(v)}_{ug} } \left(1 - f^{c(v)}_{g,j}\right)
	\right)  \nonumber\\ &&
	- \frac{ \sqrt{ f^{c}_{u,j} f^{v}_{u,j} } }{ \hat{\tau}^{c(v)}_{ur} }
  \\
  \label{eq:qd_es}
  \frac{ {\partial}f^{c(v)}_{e,j} }{ {\partial}t } & = & 
	\frac{ f^{c(v)}_{u,j} }{ \hat{\tau}^{c(v)}_{ue} }	\left(1 - f^{c(v)}_{e,j}\right) -
	\frac{ f^{c(v)}_{e,j} }{ \hat{\tau}^{c(v)}_{eu} }	\left(1 - f^{c(v)}_{u,j}\right) \nonumber\\ &&
	+ \frac{ D^{c(v)}_{g} }{ D^{c(v)}_{e} } \left(
		\frac{ f^{c(v)}_{g,j} }{ \hat{\tau}^{c(v)}_{ge} } \left(1 - f^{c(v)}_{e,j}\right)
		\frac{ f^{c(v)}_{e,j} }{ \hat{\tau}^{c(v)}_{eg} } \left(1 - f^{c(v)}_{g,j}\right)
	\right)  \nonumber\\ &&
	- \frac{ \sqrt{ f^{c}_{e,j} f^{v}_{e,j} } }{ \hat{\tau}_{dr} } - \frac{\Gamma}{N^{c(v)}_{E,j}} \nonumber\\ &&
	\times
	\sum_{k} \frac{ P_{k,in}g^{e}_{jk} \bigl[e^{( [ \Gamma g(t,E_k) - \alpha_{i} ] L_{ca})} - 1 \bigr]}{E_k\bigl[ \Gamma g \left( t,E_k \right) - \alpha_{i} \bigr]} \nonumber\\ &&
	\times
	\left( f^{c}_{e,j} + f^{v}_{e,j} - 1 \right)
  \\
  \label{eq:qd_gs}
  \frac{ {\partial}f^{c(v)}_{g,j} }{ {\partial}t } & = & 
	\frac{ f^{c(v)}_{u,j} }{ \hat{\tau}^{c(v)}_{ug} }	\left(1 - f^{c(v)}_{g,j}\right) -
	\frac{ f^{c(v)}_{g,j} }{ \hat{\tau}^{c(v)}_{gu} }	\left(1 - f^{c(v)}_{u,j}\right) \nonumber\\ &&
	\frac{ f^{c(v)}_{e,j} }{ \hat{\tau}^{c(v)}_{eg} } \left(1 - f^{c(v)}_{g,j}\right) -
	\frac{ f^{c(v)}_{g,j} }{ \hat{\tau}^{c(v)}_{ge} } \left(1 - f^{c(v)}_{e,j}\right) \nonumber\\ &&
	- \frac{ \sqrt{ f^{c}_{g,j} f^{v}_{g,j} } }{ \hat{\tau}_{dr} } - \frac{\Gamma}{N^{c(v)}_{G,j}} \nonumber\\ &&
	\times
	\sum_{k} \frac{ P_{k,in}g^{g}_{jk} \bigl[e^{( [ \Gamma g(t,E_k) - \alpha_{i} ] L_{ca})} - 1 \bigr]}{E_k\bigl[ \Gamma g \left( t,E_k \right) - \alpha_{i} \bigr]} \nonumber\\ &&
	\times
	\left( f^{c}_{g,j} + f^{v}_{g,j} - 1 \right)
\end{eqnarray}

({\ref{eq:qd_qw}})式から({\ref{eq:qd_gs}})式の左辺は活性層の各層でのキャリア占有率の時間微分を表している．ここで，各式の詳細について以下に述べる．
({\ref{eq:qd_qw}})式は微小時間におけるQWにおけるキャリア占有率の挙動である．右辺各項について説明する．
\begin{description}
  \item[式({\ref{eq:qd_qw}})第1項 $\frac{I}{qN^{c(v)}_{W}}$]\mbox{}\\
	電流励起によりQWに注入されるキャリアを表す．注入電流$I$[C/s]を電荷素量$q$[C]で割っているのは電流をキャリア数に換算するためであり，キャリア数の上限を超えないように$N^{c(v)}_{W}$で割っている．
  \item[式({\ref{eq:qd_qw}})第2項 $-\frac{ \sqrt{ f^{c}_{w} f^{v}_{w} } }{ \hat{\tau}^{c(v)}_{wr} }$]\mbox{}\\
	非発光再結合により消失するキャリアを表す．非発光再結合時間$\hat{\tau}^{c(v)}_{wr}$[s]での除算は，単位時間当たりの密度への換算であり，$\hat{\tau}^{c(v)}_{wr}$秒間隔でこの状態が発生することを表す．
  \item[式({\ref{eq:qd_qw}})第3項 $\frac{ D^{c(v)}_{u} }{ D^{c(v)}_{w} }$]\mbox{}\\
	QWに対するUSの縮退係数を表す．
  \item[式({\ref{eq:qd_qw}})第3項 $G^{c(v)}_{j}$]\mbox{}\\
	不均一広がりの内，QDのj番目の割合を表す．
  \item[式({\ref{eq:qd_qw}})第3項 $\frac{ f^{c(v)}_{u,j} }{ \hat{\tau}^{c(v)}_{uw,j} }	\left(1 - f^{c(v)}_{w} \right)$]\mbox{}\\
	USからＱＷへのキャリア脱出を表す．$1 - f^{c(v)}_{w}$ と $f^{c(v)}_{u,j}$ の乗算は，QWに脱出できる$f^{c(v)}_{u,j}$の割合を示している．キャリア脱出時間(US $\to$ QW)$\hat{\tau}^{c(v)}_{uw,j}$[s]での除算は，単位時間当たりの密度への変換であり，$\hat{\tau}^{c(v)}_{uw,j}$秒間隔でこの状態が発生することを表している．
  \item[式({\ref{eq:qd_qw}})第3項 $-\frac{ f^{c(v)}_{w}  }{ \hat{\tau}^{c(v)}_{wu}  } 	\left(1 - f^{c(v)}_{u,j}\right)$]\mbox{}\\
	ＱＷからUSへのキャリア緩和を表す．$1 - f^{c(v)}_{u,j}$ と $f^{c(v)}_{w}$ の乗算は，USに遷移できる$f^{c(v)}_{w}$の割合を示している．キャリア緩和時間(QW $\to$ US)$\hat{\tau}^{c(v)}_{wu}$[s]での除算は，単位時間当たりの密度への変換であり，$\hat{\tau}^{c(v)}_{wu}$秒間隔でこの状態が発生することを表している．
\end{description}

式({\ref{eq:qd_us}})は微小時間でのj番目のUSにおけるキャリア占有率の挙動である．右辺各項について説明する．
\begin{description}
  \item[式({\ref{eq:qd_us}})第1項 $\frac{ f^{c(v)}_{w}  }{ \hat{\tau}^{c(v)}_{wu}  }	\left(1 - f^{c(v)}_{u,j}\right)$]\mbox{}\\
	ＱＷからUSへのキャリア緩和を表す．$1 - f^{c(v)}_{u,j}$ と $f^{c(v)}_{w}$ の乗算は，USに遷移できる$f^{c(v)}_{w}$の割合を示している．キャリア緩和時間(QW $\to$ US)$\hat{\tau}^{c(v)}_{wu}$[s]での除算は，単位時間当たりの密度への変換であり，$\hat{\tau}^{c(v)}_{wu}$秒間隔でこの状態が発生することを表している．
  \item[式({\ref{eq:qd_us}})第2項 $-\frac{ f^{c(v)}_{u,j} }{ \hat{\tau}^{c(v)}_{uw,j} }	\left(1 - f^{c(v)}_{w} \right)$]\mbox{}\\
	USからＱＷへのキャリア脱出を表す．$1 - f^{c(v)}_{w}$ と $f^{c(v)}_{u,j}$ の乗算は，QWに脱出できる$f^{c(v)}_{u,j}$の割合を示している．キャリア脱出時間(US $\to$ QW)$\hat{\tau}^{c(v)}_{uw,j}$[s]での除算は，単位時間当たりの密度への変換であり，$\hat{\tau}^{c(v)}_{uw,j}$秒間隔でこの状態が発生することを表している．
  \item[式({\ref{eq:qd_us}})第3項 $\frac{ D^{c(v)}_{e} }{ D^{c(v)}_{u} }$]\mbox{}\\
	USに対するESの縮退係数を表す．
  \item[式({\ref{eq:qd_us}})第3項 $\frac{ f^{c(v)}_{e,j} }{ \hat{\tau}^{c(v)}_{eu} } \left(1 - f^{c(v)}_{u,j}\right)$]\mbox{}\\
	ESからUSへのキャリア脱出を表す．$1 - f^{c(v)}_{u,j}$ と $f^{c(v)}_{e,j}$ の乗算は，USに脱出できる$f^{c(v)}_{e,j}$の割合を示している．キャリア脱出時間(ES $\to$ US)$\hat{\tau}^{c(v)}_{eu}$[s]での除算は，単位時間当たりの密度への変換であり，$\hat{\tau}^{c(v)}_{eu}$秒間隔でこの状態が発生することを表している．
  \item[式({\ref{eq:qd_us}})第3項 $-\frac{ f^{c(v)}_{u,j} }{ \hat{\tau}^{c(v)}_{ue} } \left(1 - f^{c(v)}_{e,j}\right)$]\mbox{}\\
	USからESへのキャリア緩和を表す．$1 - f^{c(v)}_{e,j}$ と $f^{c(v)}_{u,j}$ の乗算は，ESに遷移できる$f^{c(v)}_{u,j}$の割合を示している．キャリア緩和時間(US $\to$ ES)$\hat{\tau}^{c(v)}_{ue}$[s]での除算は，単位時間当たりの密度への変換であり，$\hat{\tau}^{c(v)}_{ue}$秒間隔でこの状態が発生することを表している．
  \item[式({\ref{eq:qd_us}})第4項 $\frac{ D^{c(v)}_{g} }{ D^{c(v)}_{u} }$]\mbox{}\\
	USに対するGSの縮退係数を表す．
  \item[式({\ref{eq:qd_us}})第4項 $\frac{ f^{c(v)}_{g,j} }{ \hat{\tau}^{c(v)}_{gu} } \left(1 - f^{c(v)}_{u,j}\right)$]\mbox{}\\
	GSからUSへのキャリア脱出を表す．$1 - f^{c(v)}_{u,j}$ と $f^{c(v)}_{g,j}$ の乗算は，USに脱出できる$f^{c(v)}_{g,j}$の割合を示している．キャリア脱出時間(GS $\to$ US)$\hat{\tau}^{c(v)}_{gu}$[s]での除算は，単位時間当たりの密度への変換であり，$\hat{\tau}^{c(v)}_{gu}$秒間隔でこの状態が発生することを表している．
  \item[式({\ref{eq:qd_us}})第4項 $-\frac{ f^{c(v)}_{u,j} }{ \hat{\tau}^{c(v)}_{ug} } \left(1 - f^{c(v)}_{g,j}\right)$]\mbox{}\\
	USからGSへのキャリア緩和を表す．$1 - f^{c(v)}_{g,j}$ と $f^{c(v)}_{u,j}$ の乗算は，GSに遷移できる$f^{c(v)}_{u,j}$の割合を示している．キャリア緩和時間(US $\to$ GS)$\hat{\tau}^{c(v)}_{ug}$[s]での除算は，単位時間当たりの密度への変換であり，$\hat{\tau}^{c(v)}_{ug}$秒間隔でこの状態が発生することを表している．
  \item[式({\ref{eq:qd_us}})第5項 $-\frac{ \sqrt{ f^{c}_{u,j} f^{v}_{u,j} } }{ \hat{\tau}^{c(v)}_{ur} }$]\mbox{}\\
	非発光再結合により消失するキャリアを表す．非発光再結合時間$\hat{\tau}^{c(v)}_{ur}$[s]での除算は，単位時間当たりの密度への換算であり，$\hat{\tau}^{c(v)}_{ur}$秒間隔でこの状態が発生することを表す．
\end{description}

式({\ref{eq:qd_es}})は微小時間でのj番目のESにおけるキャリア占有率の挙動である．右辺各項について説明する．
\begin{description}
  \item[式({\ref{eq:qd_es}})第1項 $\frac{ f^{c(v)}_{u,j} }{ \hat{\tau}^{c(v)}_{ue} } \left(1 - f^{c(v)}_{e,j}\right)$]\mbox{}\\
	USからESへのキャリア緩和を表す．$1 - f^{c(v)}_{e,j}$ と $f^{c(v)}_{u,j}$ の乗算は，ESに遷移できる$f^{c(v)}_{u,j}$の割合を示している．キャリア緩和時間(US $\to$ ES)$\hat{\tau}^{c(v)}_{ue}$[s]での除算は，単位時間当たりの密度への変換であり，$\hat{\tau}^{c(v)}_{ue}$秒間隔でこの状態が発生することを表している．
  \item[式({\ref{eq:qd_es}})第2項 $-\frac{ f^{c(v)}_{e,j} }{ \hat{\tau}^{c(v)}_{eu} } \left(1 - f^{c(v)}_{u,j}\right)$]\mbox{}\\
	ESからUSへのキャリア脱出を表す．$1 - f^{c(v)}_{u,j}$ と $f^{c(v)}_{e,j}$ の乗算は，USに脱出できる$f^{c(v)}_{e,j}$の割合を示している．キャリア脱出時間(ES $\to$ US)$\hat{\tau}^{c(v)}_{eu}$[s]での除算は，単位時間当たりの密度への変換であり，$\hat{\tau}^{c(v)}_{eu}$秒間隔でこの状態が発生することを表している．
  \item[式({\ref{eq:qd_es}})第3項 $\frac{ D^{c(v)}_{g} }{ D^{c(v)}_{e} }$]\mbox{}\\
	ESに対するGSの縮退係数を表す．
  \item[式({\ref{eq:qd_es}})第3項 $\frac{ f^{c(v)}_{g,j} }{ \hat{\tau}^{c(v)}_{ge} } \left(1 - f^{c(v)}_{e,j}\right)$]\mbox{}\\
	GSからESへのキャリア脱出を表す．$1 - f^{c(v)}_{e,j}$ と $f^{c(v)}_{g,j}$ の乗算は，ESに脱出できる$f^{c(v)}_{g,j}$の割合を示している．キャリア脱出時間(GS $\to$ ES)$\hat{\tau}^{c(v)}_{ge}$[s]での除算は，単位時間当たりの密度への変換であり，$\hat{\tau}^{c(v)}_{ge}$秒間隔でこの状態が発生することを表している．
  \item[式({\ref{eq:qd_es}})第3項 $-\frac{ f^{c(v)}_{e,j} }{ \hat{\tau}^{c(v)}_{eg} } \left(1 - f^{c(v)}_{g,j}\right)$]\mbox{}\\
	ESからGSへのキャリア緩和を表す．$1 - f^{c(v)}_{g,j}$ と $f^{c(v)}_{e,j}$ の乗算は，GSに遷移できる$f^{c(v)}_{e,j}$の割合を示している．キャリア緩和時間(ES $\to$ GS)$\hat{\tau}^{c(v)}_{eg}$[s]での除算は，単位時間当たりの密度への変換であり，$\hat{\tau}^{c(v)}_{eg}$秒間隔でこの状態が発生することを表している．
  \item[式({\ref{eq:qd_es}})第4項 $-\frac{ \sqrt{ f^{c}_{e,j} f^{v}_{e,j} } }{ \hat{\tau}^{c(v)}_{dr} }$]\mbox{}\\
	非発光再結合により消失するキャリアを表す．非発光再結合時間$\hat{\tau}^{c(v)}_{dr}$[s]での除算は，単位時間当たりの密度への換算であり，$\hat{\tau}^{c(v)}_{dr}$秒間隔でこの状態が発生することを表す．
  \item[式({\ref{eq:qd_es}})第5項 $-\frac{\Gamma}{N^{c(v)}_{E,j}} \sum_{k} \frac{ P_{k,in}g^{e}_{jk} \bigl[e^{( [ \Gamma g(t,E_k) - \alpha_{i} ] L_{ca})} - 1 \bigr]}{E_k\bigl[ \Gamma g \left( t,E_k \right) - \alpha_{i} \bigr]} \left( f^{c}_{e,j} + f^{v}_{e,j} - 1 \right)$]\mbox{}\\
	誘導放出により消失するキャリア及び誘導吸収により増加するキャリアを表す．
\end{description}

式({\ref{eq:qd_gs}})は微小時間でのj番目のGSにおけるキャリア占有率の挙動である．右辺各項について説明する．
\begin{description}
  \item[式({\ref{eq:qd_gs}})第1項 $\frac{ f^{c(v)}_{u,j} }{ \hat{\tau}^{c(v)}_{ug} } \left(1 - f^{c(v)}_{g,j}\right)$]\mbox{}\\
	USからGSへのキャリア緩和を表す．$1 - f^{c(v)}_{e,j}$ と $f^{c(v)}_{u,j}$ の乗算は，GSに遷移できる$f^{c(v)}_{u,j}$の割合を示している．キャリア緩和時間(US $\to$ GS)$\hat{\tau}^{c(v)}_{ug}$[s]での除算は，単位時間当たりの密度への変換であり，$\hat{\tau}^{c(v)}_{ug}$秒間隔でこの状態が発生することを表している．
  \item[式({\ref{eq:qd_gs}})第2項 $-\frac{ f^{c(v)}_{g,j} }{ \hat{\tau}^{c(v)}_{gu} } \left(1 - f^{c(v)}_{u,j}\right)$]\mbox{}\\
	GSからUSへのキャリア脱出を表す．$1 - f^{c(v)}_{g,j}$ と $f^{c(v)}_{g,j}$ の乗算は，USに脱出できる$f^{c(v)}_{g,j}$の割合を示している．キャリア脱出時間(GS $\to$ US)$\hat{\tau}^{c(v)}_{gu}$[s]での除算は，単位時間当たりの密度への変換であり，$\hat{\tau}^{c(v)}_{gu}$秒間隔でこの状態が発生することを表している．
  \item[式({\ref{eq:qd_gs}})第3項 $\frac{ f^{c(v)}_{e,j} }{ \hat{\tau}^{c(v)}_{eg} } \left(1 - f^{c(v)}_{g,j}\right)$]\mbox{}\\
	ESからGSへのキャリア緩和を表す．$1 - f^{c(v)}_{g,j}$ と $f^{c(v)}_{e,j}$ の乗算は，GSに遷移できる$f^{c(v)}_{e,j}$の割合を示している．キャリア緩和時間(ES $\to$ GS)$\hat{\tau}^{c(v)}_{eg}$[s]での除算は，単位時間当たりの密度への変換であり，$\hat{\tau}^{c(v)}_{eg}$秒間隔でこの状態が発生することを表している．
  \item[式({\ref{eq:qd_gs}})第4項 $-\frac{ f^{c(v)}_{g,j} }{ \hat{\tau}^{c(v)}_{ge} } \left(1 - f^{c(v)}_{e,j}\right)$]\mbox{}\\
	GSからESへのキャリア脱出を表す．$1 - f^{c(v)}_{e,j}$ と $f^{c(v)}_{g,j}$ の乗算は，ESに脱出できる$f^{c(v)}_{g,j}$の割合を示している．キャリア脱出時間(GS $\to$ ES)$\hat{\tau}^{c(v)}_{ge}$[s]での除算は，単位時間当たりの密度への変換であり，$\hat{\tau}^{c(v)}_{ge}$秒間隔でこの状態が発生することを表している．
  \item[式({\ref{eq:qd_gs}})第5項 $-\frac{ \sqrt{ f^{c}_{g,j} f^{v}_{g,j} } }{ \hat{\tau}^{c(v)}_{dr} }$]\mbox{}\\
	非発光再結合により消失するキャリアを表す．非発光再結合時間$\hat{\tau}^{c(v)}_{dr}$[s]での除算は，単位時間当たりの密度への換算であり，$\hat{\tau}^{c(v)}_{dr}$秒間隔でこの状態が発生することを表す．
  \item[式({\ref{eq:qd_gs}})第6項 $-\frac{\Gamma}{N^{c(v)}_{G,j}} \sum_{k} \frac{ P_{k,in}g^{g}_{jk} \bigl[e^{( [ \Gamma g(t,E_k) - \alpha_{i} ] L_{ca})} - 1 \bigr]}{E_k\bigl[ \Gamma g \left( t,E_k \right) - \alpha_{i} \bigr]} \left( f^{c}_{g,j} + f^{v}_{g,j} - 1 \right)$]\mbox{}\\
	誘導放出により消失するキャリア及び誘導吸収により増加するキャリアを表す．
\end{description}

活性層の各層で存在できるキャリアの数には上限があり，QWからGSまで各層のキャリア数の上限は({\ref{eq:qw_max}})式から({\ref{eq:gs_max}})式で表される．
\begin{eqnarray}
  \label{eq:qw_max}
    N^{c(v)}_{W} & = & N_{QD}D^{c(v)}_{w} \\
  \label{eq:us_max}
    N^{c(v)}_{U,j} & = & N_{QD}D^{c(v)}_{u} G^{c(v)}_{j} \\
  \label{eq:es_max}
    N^{c(v)}_{E,j} & = & N_{QD}D^{c(v)}_{e} G^{c(v)}_{j} \\
  \label{eq:gs_max}
    N^{c(v)}_{G,j} & = & N_{QD}D^{c(v)}_{g} G^{c(v)}_{j}
\end{eqnarray}

\newpage

\subsection{QD-SOAによる利得}
QD-SOAの単位長さあたりの利得$g(t,E_k)$は
\begin{equation}
  \label{eq:gain}
  g(t,E_k) = \sum_{l=e,g}\sum_{j=1}^{2M+1} g^{l}_{j,k}(E_k)\bigl[f^{c}_{l,j}+f^{v}_{l,j}-1\bigr]
\end{equation}
で表される．これは，単位長さあたりの利得$g(t,E_k)$がES，GSのキャリア占有率に依存して変化することを示している．\\
また，j番目ES，GS層が波長kの光子に与える利得$g^{e(g)}_{j,k}$は
\begin{equation}
  g^{e(g)}_{j,k} = \frac{ \pi \hbar q^{2} N_{D} D_{e(g)} }{ c n_{r} \varepsilon_{0} m^{2}_{0} E_{k} } \left|M_{env}\right|^{2} \left<\left|\hat{e} \cdot p_{cv}\right|^{2}\right> L^{e(g)}_{j} G^{s}_{j}
\end{equation}
で表される．ここで，$L^{e(g)}_{j}$，$G^{s}_{j}$はそれぞれ光子エネルギーの均一広がり，j番目QD層の放射再結合の割合を表しており，
\begin{eqnarray}
  L^{e(g)}_{j}  	& = & \frac{1}{\pi} \frac{ \Gamma_{h} / 2 }{\left(E^{s}_{e(g),j} - E_k\right)^{2} + \left(\Gamma_{h}/2\right)^{2}} \\
  G^{s}_{j}  	& = & \frac{1}{\sqrt{2 \pi \sigma^{2}_{in,s}}} \exp \left[-\frac{\left(E^{s}_{g,j}-E^{s}_{g,M}\right)^{2}}{2\sigma^{2}_{in,s}}\right] \Delta E
\end{eqnarray}
と表される．また$E^{s}_{u(e,g),j}$はQD各層における光子遷移エネルギーであり，
\begin{equation}
  E^{s}_{u(e,g),j} = E^{c}_{u(e,g),j} - E^{v}_{u(e,g),j}
\end{equation}
となる．このとき，出力光強度$P_{k,out}$は増幅利得を$G(t,E_k)$として入力強度を$P_{k,in}$とすると
\begin{equation}
  P_{k,out} = G(t,E_k)P_{k,in}
\end{equation}
と表される．また，増幅利得は閉じ込め係数を$\Gamma$，損失係数を$\alpha_{i}$，活性層の長さを$L_{ca}$として({\ref{eq:total_gain}})式で表される．
\begin{equation}
  \label{eq:total_gain}
  G(t,E_k) = \exp\left(\left[\Gamma g(t,E_k) - \alpha_{i}\right]L_{ca}\right)
\end{equation}

\subsection{QD-SOAによる位相変化}
QD-SOAはバンド充填効果，自由電子吸収効果により屈折率変化が起き，それにより光の位相変化が起きる．以下にバンド充填効果による屈折率変化の式を示す．
\begin{eqnarray}
  \Delta n^{w}_{ib} (t,E_k) & = & 
	\frac
	{-\Gamma_{w} \pi \hbar^{2} q^{2} N_{D}}
	{2 n_{r} \varepsilon_{0} m^{2}_{0} E^{2}_{k}}
	\left|M_{env}\right|^{2} \left<\left|\hat{e} \cdot p_{cv}\right|^{2}\right> \nonumber\\ &&
  	\times 
	\frac
	{D^{c}_{w} D^{v}_{w}}
	{D^{c}_{w} + D^{v}_{w}} 
	B_{w}\left[f^{c}_{w}+f^{v}_{w}-1\right]
  \\
  \Delta n^{u}_{ib} (t,E_k) & = & 
	\sum_{j=1}^{2M+1} 
	\frac
	{-\Gamma \pi \hbar^{2} q^{2} N_{D}}
	{2 n_{r} \varepsilon_{0} m^{2}_{0} E^{2}_{k}} 
	\left|M_{env}\right|^{2}
	\left<\left|\hat{e} \cdot p_{cv}\right|^{2}\right> \nonumber\\ &&
  	\times \frac
	{ D^{c}_{u} D^{v}_{u} }
	{D^{c}_{u} + D^{v}_{u}}
	G^{s}_{j}B_{u,j}\left[f^{c}_{u,j}+f^{v}_{u,j}-1\right]
  \\
  \Delta n^{e(g)}_{ib} (t,E_k) & = &
	\sum_{j=1}^{2M+1} 
	\frac
	{-\Gamma \pi \hbar^{2} q^{2} N_{D} D_{e(g)} }
	{2 n_{r} \varepsilon_{0} m^{2}_{0} E^{2}_{k}}
	\left|M_{env}\right|^{2}
	\left<\left|\hat{e} \cdot p_{cv}\right|^{2}\right> \nonumber\\ &&
  	\times G^{s}_{j} B_{e(g),j}	\left[f^{c}_{e(g),j}+f^{v}_{e(g),j}-1\right]
\end{eqnarray}
ここで$B_{w}$，$B_{u(e,g),j}$は屈折率の均一広がりを表し，以下の式で与えられる．
\begin{eqnarray}
  B_{w} & = & \frac{\left(E^{s}_{w} - E_{k}\right) / \pi}{\left(E^{s}_{w} - E_{k}\right)^{2} + \left(\Gamma_{h} / 2\right)^{2}} \\
  B_{u(e,g),j} & = & \frac{\left(E^{s}_{u(e,g),j} - E_{k}\right) / \pi}{\left(E^{s}_{u(e,g),j} - E_{k}\right)^{2} + \left(\Gamma_{h} / 2\right)^{2}}
\end{eqnarray}
自由電子吸収効果による屈折率変化の式は以下のようになる．
\begin{eqnarray}
  \Delta n^{w}_{fc} (t,E_k) & = & - \frac{ \Gamma_{w} \hbar^{2} q^{2} }{ 2 V_{w} n_{r} \varepsilon_{0} E^{2}_{k} } 
  \left(
	\frac{ N^{c}_{W} }{ m^{*c}_{w} } f^{c}_{w} + 
	\frac{ N^{v}_{W} }{ m^{*v}_{w} } f^{v}_{w}
  \right) 
  \\
  \Delta n^{u(e,g)}_{fc} (t,E_k) & = & - \frac{ \Gamma \hbar^{2} q^{2} }{ 2 V_{d} n_{r} \varepsilon_{0} E^{2}_{k} } \nonumber\\ &&
  \left( \sum_{j=1}^{2M+1} 
	  \frac{ f^{c}_{u(e,g),j} N^{c}_{U(E,G),j} }{ m^{*c}_{d} } 
	+\frac{ f^{v}_{u(e,g),j} N^{v}_{U(E,G),j} }{ m^{*v}_{d} }
  \right)
\end{eqnarray}
バンド充填効果，自由電子吸収効果による屈折率変化量の和を$\Delta n(t,E_k)$とすると位相変化量は
\begin{equation}
  \label{eq:phase_diff}
  \Delta \phi (t,E_k) = -\frac{2 \pi L_{ca}}{\lambda_{k}} \Delta n(t,E_k)
\end{equation}
と表される．

\newpage

\subsection{QD-SOA内の光の伝搬方程式}

QD-SOA内の光の伝搬方程式は単位長さあたりの増幅利得を$dG(t,E_k)$，位相変化量を$d\phi (t,E_k)$とすると
\begin{equation}
  \label{eq:denpan}
  \frac{{\partial}E}{{\partial}z} = E\left(\frac{dG(t,E_k)}{2}+jd\phi (t,E_k)\right)
\end{equation}
で表せる．$z$はQD-SOA内の光の伝搬方向の位置，$j$は虚数単位を表している．

\subsection{非線形状態空間法(NSSM:Nonlinear State Space Method)}
QD-SOAの入出力特性の解析にはNSSM{\cite{NSSM}}を用いる．NSSMとはQD-SOAに入力する光からQD-SOAから出力される光を直接求める手法で，計算時間の短縮を行うことができる．光の伝搬方程式は(\ref{eq:denpan})式で表され，光が伝搬方向に沿って$\Delta z$だけ進んだときの電場は$z$の位置の電場を$E(z)$とすると
\begin{equation}
  \label{eq:denpan}
  E(z+\Delta z) = E(z)\exp\left(dG(t,E_k)+jd\phi (t,E_k)\right)
\end{equation}
と表される．また，式(\ref{eq:total_gain})，式(\ref{eq:phase_diff})よりQD-SOAに入力する光の電界を$E(0)$とするとQD-SOAから出力される光の電界$E(L_{ca})$は(\ref{eq:electric_field_out})式より求められる．
\begin{equation}
  \label{eq:electric_field_out}
  E(L_{ca}) = E(0)\exp\left(G(t,E_{k})+j\Delta \phi (t,E_{k})\right)
\end{equation}

\newpage

\section{フォトニック結晶(PC:Photonic Crystal)}
フォトニック結晶とは屈折率の異なる物質が周期的に並んだナノ構造体である{\cite{photonic_crystal}}．1～3次元の結晶が構築可能で，フォトニック結晶中の光の伝わり方はナノ構造によって制御できる．フォトニック結晶は光の回折，散乱，干渉を利用し，その特性を利用した光ファイバ，光導波路，光メモリが存在する．

\section{分散関係}
分散関係は角振動数と波数の間の関係を表し，波数$k$に対して一定の角振動数$\omega(k)$が成立する．分散がない場合では，波数と角振動数が比例関係$\omega(k) = vk$が成立する．例えば，真空中であれば分散がないので真空中の光速を$c_{0}$とすると分散関係$\omega(k) = c_{0}k$が成り立つ．

\section{スローライト}
波の位相部分が一定$kx - \omega t = \phi_{0}$で伝わる速度は位相速度という．位相速度$v_{p}$は
\begin{equation}
  v_{p} = \frac{\partial x}{\partial t} = \frac{\omega(k)}{k}
\end{equation}
と表される．また，様々な波数を持つ波の集まりである波束の速度は群速度といい，群速度$v_{g}$は
\begin{equation}
  v_{g} = \frac{\partial \omega(k)}{\partial k}
\end{equation}
と表される．このとき，スローダウン要素は$S(\lambda)=v_{p}/v_{g}$と表される．

\section{フォトニック結晶導波路(PCW:Photonic Crystal Waveguides)}
フォトニック結晶導波路は2次元のフォトニック結晶で，図{\ref{fig:model_of_pcw}}のように三角格子状に空孔が並んだ結晶に一列分の欠陥を作ることで光が欠陥部分を伝搬できるようにする．特定の波長の光は結晶部分を伝搬することができないため，導波路中を低損失で伝搬することができる．また，格子係数を変えることで分散関係により群速度を小さくして光の伝搬速度を遅くすることができる．

\begin{figure}[htbp]
  \begin{center}
    \includegraphics[clip,width=12.0cm]{model_of_pcw.eps}
    \caption{フォトニック結晶導波路のモデル}
    \label{fig:model_of_pcw}
  \end{center}
\end{figure}

\section{フォトニック結晶量子ドット構造半導体光増幅器(PC-QDSOA)}
PC-QDSOAとはQD-SOAのクラッド部分をPCにしてQD-SOAの構造をPCWにしたもので，フォトニック結晶のスローライト効果により光の速度を遅くすることによってQD-SOAと比べて短いデバイス長でQD-SOAと同等の利得を得ることができる．また，活性層の幅がQD-SOAよりも狭いため，増幅に必要な注入電流量がQD-SOAよりも小さい．これらの利点から，PC-QDSOAはQD-SOAと比較して小容量化が可能であり，省電力化が可能であるといえる{\cite{model_of_pc_qdsoa}}．図{\ref{fig:model_of_pc_qdsoa}}にPC-QDSOAのモデル図を示す．

\begin{figure}[htbp]
  \begin{center}
    \includegraphics[clip,width=12.0cm]{model_of_pcw.eps}
    \caption{PC-QDSOAのモデル}
    \label{fig:model_of_pc_qdsoa}
  \end{center}
\end{figure}

\section{PC-QDSOAの入出力特性の解析}
ここでは，PC-QDSOAの入出力特性の解析に用いる手法，式について説明する．

\subsection{誘電体の屈折率}
誘電体の屈折率は構成する媒体の屈折率の平均から求めることができ，屈折率の平均は媒体の体積比によって決まる．PC-QDSOAはGaAs，${\rm In_{0.15}Ga_{0.85}As }$の屈折率及び体積比から求めることができる．また，媒体の屈折率は波長依存性をもち，GaAsの屈折率$n_{\rm GaAs}(\lambda)$は波長$\lambda$に対して
\begin{equation}
  n_{\rm GaAs}(\lambda) = \left[10.906 + \frac{0.97501}{\lambda^{2}-0.2797}-0.002467\lambda^{2}\right]^{1/2}
\end{equation}
と表される{\cite{GaAsIndex}}．
${\rm In_{x}Ga_{1-x}As }$の屈折率$n{\rm In_{x}Ga_{1-x}As }(\lambda,x)$は波長$\lambda$及びインジウムの構成比$x$に対して
\begin{equation}
  n{\rm In_{x}Ga_{1-x}As }(\lambda,x) = \sqrt{8.950+\frac{ 2.054 }{ 1-\left(\frac{0.6254\times E_{GaAs}}{\lambda \cdot E_{g}(x)}\right) }}
\end{equation}
と表される{\cite{InGaAsIndex}}．また，${\rm In_{x}Ga_{1-x}As }$のバンドギャップエネルギー$E_{g}(x)$は(\ref{eq:InGaAs_bandgap})式より求めることができる．
\begin{equation}
  \label{eq:InGaAs_bandgap}
  E_{g}(x) = E_{g_{GaAs}} - (1.501)x + (0.436)x^{2}
\end{equation}
ここで，PC-QDSOAにおけるGaAsの割合を$V_{\rm GaAs}$，${\rm In_{x}Ga_{1-x}As}$の割合を$V_{\rm In_{x}Ga_{1-x}As}$とすると誘電体の屈折率$n_{core}(\lambda)$は
\begin{equation}
  n_{core}(\lambda) = \frac
	{n_{\rm GaAs}(\lambda) V_{\rm GaAs} + n{\rm In_{x}Ga_{1-x}As }(\lambda,x) V_{\rm In_{x}Ga_{1-x}As}}
	{V_{\rm GaAs} + V_{\rm In_{x}Ga_{1-x}As}}
\end{equation}
と表される．

\subsection{PC-QD-SOAのレート方程式}
PC-QD-SOAの解析にはQD-SOAと同様にレート方程式を用いる．本研究に用いるPC-QD-SOAのレート方程式を式に示す{\cite{pc_qd_soa_rate_equation}}．また，各項の意味はQD-SOAと同等である．

\begin{eqnarray}
  \label{eq:pc_qd_qw}
  \frac{ {\partial}f^{c(v)}_{w} }{ {\partial}t } & = & 
	\frac{I}{qN^{c(v)}_{W}} - 
	\frac{ \sqrt{ f^{c}_{w} f^{v}_{w} } }{ \hat{\tau}^{c(v)}_{wr} } + 
	\frac{ D^{c(v)}_{u} }{ D^{c(v)}_{w} } \sum_{j=1}^{2M+1} G^{c(v)}_{j} \nonumber\\ &&
	\times
	\left(
		\frac{ f^{c(v)}_{u,j} }{ \hat{\tau}^{c(v)}_{uw,j} }	\left(1 - f^{c(v)}_{w} \right)
		\frac{ f^{c(v)}_{w}  }{ \hat{\tau}^{c(v)}_{wu}  } 	\left(1 - f^{c(v)}_{u,j}\right)
	\right)
  \\
  \label{eq:pc_qd_us}
  \frac{ {\partial}f^{c(v)}_{u,j} }{ {\partial}t } & = & 
	\frac{ f^{c(v)}_{w}  }{ \hat{\tau}^{c(v)}_{wu}  }	\left(1 - f^{c(v)}_{u,j}\right) -
	\frac{ f^{c(v)}_{u,j} }{ \hat{\tau}^{c(v)}_{uw,j} }	\left(1 - f^{c(v)}_{w} \right) \nonumber\\ &&
	+ \frac{ D^{c(v)}_{e} }{ D^{c(v)}_{u} } \left(
		\frac{ f^{c(v)}_{e,j} }{ \hat{\tau}^{c(v)}_{eu} } \left(1 - f^{c(v)}_{u,j}\right)
		\frac{ f^{c(v)}_{u,j} }{ \hat{\tau}^{c(v)}_{ue} } \left(1 - f^{c(v)}_{e,j}\right)
	\right)  \nonumber\\ &&
	+ \frac{ D^{c(v)}_{g} }{ D^{c(v)}_{u} } \left(
		\frac{ f^{c(v)}_{g,j} }{ \hat{\tau}^{c(v)}_{gu} } \left(1 - f^{c(v)}_{u,j}\right)
		\frac{ f^{c(v)}_{u,j} }{ \hat{\tau}^{c(v)}_{ug} } \left(1 - f^{c(v)}_{g,j}\right)
	\right)  \nonumber\\ &&
	- \frac{ \sqrt{ f^{c}_{u,j} f^{v}_{u,j} } }{ \hat{\tau}^{c(v)}_{ur} }
  \\
  \label{eq:pc_qd_es}
  \frac{ {\partial}f^{c(v)}_{e,j} }{ {\partial}t } & = & 
	\frac{ f^{c(v)}_{u,j} }{ \hat{\tau}^{c(v)}_{ue} }	\left(1 - f^{c(v)}_{e,j}\right) -
	\frac{ f^{c(v)}_{e,j} }{ \hat{\tau}^{c(v)}_{eu} }	\left(1 - f^{c(v)}_{u,j}\right) \nonumber\\ &&
	+ \frac{ D^{c(v)}_{g} }{ D^{c(v)}_{e} } \left(
		\frac{ f^{c(v)}_{g,j} }{ \hat{\tau}^{c(v)}_{ge} } \left(1 - f^{c(v)}_{e,j}\right)
		\frac{ f^{c(v)}_{e,j} }{ \hat{\tau}^{c(v)}_{eg} } \left(1 - f^{c(v)}_{g,j}\right)
	\right)  \nonumber\\ &&
	- \frac{ 1 }{ N^{c(v)}_{E,j} } \sum_{k} \frac
		{\Gamma_{k}P_{k,in}g^{e}_{j,k}\left[e^{\left(\left[g_{mod}(t,\lambda_{k}) - \alpha(\lambda_{k})\right]L_{ca}\right)} - 1\right]}
		{\hbar \omega_{k}\left[g_{mod}(t,\lambda_{k}) - \alpha(\lambda_{k})\right]} \nonumber\\ &&
	\times
	\left( f^{c}_{e,j} + f^{v}_{e,j} - 1 \right)
  \\
  \label{eq:pc_qd_gs}
  \frac{ {\partial}f^{c(v)}_{g,j} }{ {\partial}t } & = & 
	\frac{ f^{c(v)}_{u,j} }{ \hat{\tau}^{c(v)}_{ug} }	\left(1 - f^{c(v)}_{g,j}\right) -
	\frac{ f^{c(v)}_{g,j} }{ \hat{\tau}^{c(v)}_{gu} }	\left(1 - f^{c(v)}_{u,j}\right) \nonumber\\ &&
	\frac{ f^{c(v)}_{e,j} }{ \hat{\tau}^{c(v)}_{eg} } \left(1 - f^{c(v)}_{g,j}\right) -
	\frac{ f^{c(v)}_{g,j} }{ \hat{\tau}^{c(v)}_{ge} } \left(1 - f^{c(v)}_{e,j}\right) \nonumber\\ &&
	- \frac{ \sqrt{ f^{c}_{g,j} f^{v}_{g,j} } }{ \hat{\tau}_{dr} } - \frac{1}{N^{c(v)}_{G,j}} \nonumber\\ &&
	\times \left(\sum_{k} 
	\frac
		{\Gamma_{k}P_{k,in}g^{g}_{j,k}\left[e^{\left(\left[g_{mod}(t,\lambda_{k}) - \alpha(\lambda_{k})\right]L_{ca}\right)} - 1\right]}
		{\hbar \omega_{k}\left[g_{mod}(t,\lambda_{k}) - \alpha(\lambda_{k})\right]} + \right. \nonumber\\ && 
	\left.
	\frac
		{\Gamma_{probe}P_{probe}g^{e}_{j,probe}\left[e^{\left(\left[g_{mod}(t,\lambda_{probe}) - \alpha(\lambda_{probe})\right]L_{ca}\right)} - 1\right]}
		{\hbar \omega_{probe}\left[g_{mod}(t,\lambda_{probe}) - \alpha(\lambda_{probe})\right]}
	\right) \nonumber\\ &&
	\times
	\left( f^{c}_{g,j} + f^{v}_{g,j} - 1 \right)
\end{eqnarray}

\subsection{PC-QDSOAにおける閉じ込め係数}
PC-QDSOAでは垂直方向に関する光閉じ込めに加えて，PCにより水平方向に関する光閉じ込めが行われる．垂直方向に関する波数を$k_{z}$，活性層の厚さを$2d$とすると超越方程式
\begin{equation}
  \tan(k_{z}d) = \sqrt{\frac
	{\omega^{2} \mu_{0} (\varepsilon_{core}-\varepsilon_{cladding}) d^{2}}
	{(k_{z}d)^{2}} - 1}
\end{equation}
から垂直方向に関する光閉じ込め係数$\Gamma_{z}$を以下の式より求めることができる．
\begin{equation}
  \Gamma_{z} = \left[1 + \frac
	{\cot(k_{z}d/2)\cos^{2}(k_{z}d/2)}
	{(k_{z}d/2)(1+\sin(k_{z}d)/(k_{z}d))}\right]^{-1}
\end{equation}
水平方向に関する光閉じ込め係数$\Gamma_{xy}$は導波路幅を$W$，格子定数を$a$として以下の式より求めることができる．
\begin{equation}
  \Gamma_{xy} = \frac
	{\int_{-W/2}^{W/2} \int_{-a/2}^{a/2} \frac{1}{2}Re\{E_{t} \times H_{z}^{*}\}.\hat{x}dxdy}
	{\iint_{super cell}\frac{1}{2}Re\{E_{t} \times H_{z}^{*}\}.\hat{x}dxdy}
\end{equation}
以上が導波路に関する閉じ込め係数$\Gamma_{PCW} = \Gamma_{xy} \Gamma_{z}$であり，QD部に影響する閉じ込め係数$\Gamma(\lambda)$は割合は体積の比率$\xi_{QD} = l_{D}L_{H}/2d$から求めることができ，
\begin{equation}
  \Gamma(\lambda) = \Gamma_{PCW} \xi_{QD}
\end{equation}
と表される．

\subsection{PC-QDSOAにおける損失係数}
PC-QDSOAでは光の散乱，吸収により光が損失する．散乱による損失を$\alpha_{scattering}(\lambda)$，吸収による損失を$\alpha_{absorption}(\lambda)$とすると損失係数はそれらの和$\alpha(\lambda) = \alpha_{scattering}(\lambda) + \alpha_{absorption}(\lambda)$で表される．
また，$\alpha_{scattering}(\lambda)$，$\alpha_{absorption}(\lambda)$はそれぞれ
\begin{eqnarray}
  \alpha_{scattering}(\lambda) & = & \alpha_{intrinsic}S(\lambda) + \alpha_{extrinsic}S^{2}(\lambda) \\
  \alpha_{absorption}(\lambda) & = & \Gamma_{z}S(\lambda)\left[\Gamma_{xy}\alpha_{core} + (1-\Gamma_{xy})\alpha_{cladding}\right]
\end{eqnarray}
と求められる．

\subsection{PC-QDSOAによる利得}
PC-QDSOAの単位長さあたりの利得$g_{mod}(t,\lambda_{k})$はスローダウン要素，閉じ込め係数の波長依存性を考慮して
\begin{equation}
  g_{mod}(t,\lambda_{k}) = S(\lambda_{k})\Gamma(\lambda_{k})g_{mat}(t,\lambda_{k})
\end{equation}
と表せる．
このとき，出力光強度$P_{k,out}$は増幅利得を$G(t,E_k)$として入力強度を$P_{k,in}$とすると
\begin{equation}
  P_{k,out} = G(t,E_k)P_{k,in}
\end{equation}
と表される．また，増幅利得$G(t,E_k)$は閉じ込め係数を$\Gamma$，損失係数を$\alpha_{i}$，活性層の長さを$L_{ca}$として
\begin{equation}
  \label{eq:pc_total_gain}
  G(t,E_k) = \exp\left(\left[g_{mod}(t,\lambda_{k}) - \alpha(\lambda_{k})\right]L_{ca}\right)
\end{equation}
と表される．

\subsection{PC-QDSOAによる位相変化}
PC-QDSOAはバンド充填効果，自由電子吸収効果により屈折率変化が起き，それにより光の位相変化が起きる．以下にバンド充填効果による屈折率変化の式を示す．
\begin{eqnarray}
  \Delta n^{w}_{ib} (t,\lambda_{k}) & = & -S(\lambda_{k})\Gamma_{w}(\lambda_{k}) 
	\frac
	{\pi \hbar^{2} q^{2} N_{D}}
	{2 n_{r} \varepsilon_{0} m^{2}_{0} E^{2}_{k}} \nonumber\\ &&
	\left|M_{env}\right|^{2} \left<\left|\hat{e} \cdot p_{cv}\right|^{2}\right> 
  	\frac
	{ D^{c}_{w} D^{v}_{w} }
	{D^{c}_{w} + D^{v}_{w}}
	B_{w}\left[f^{c}_{w}+f^{v}_{w}-1\right]
  \\
  \Delta n^{u}_{ib} (t,\lambda_{k}) & = & -S(\lambda{k})\Gamma(\lambda_{k}) \sum_{j=1}^{2M+1} 
	\frac
	{\pi \hbar^{2} q^{2} N_{D}}
	{2 n_{r} \varepsilon_{0} m^{2}_{0} E^{2}_{k}} \nonumber\\ &&
	\times \left|M_{env}\right|^{2} \left<\left|\hat{e} \cdot p_{cv}\right|^{2}\right>
	\frac
	{D^{c}_{u} D^{v}_{u}}
	{D^{c}_{u} + D^{v}_{u}}
	G^{s}_{j}B_{u,j}\left[f^{c}_{u,j}+f^{v}_{u,j}-1\right]
  \\
  \Delta n^{e(g)}_{ib} (t,\lambda_{k}) & = & -S(\lambda{k})\Gamma(\lambda_{k}) \sum_{j=1}^{2M+1} 
	\frac
	{\pi \hbar^{2} q^{2} N_{D} D_{e(g)}}
	{2 n_{r} \varepsilon_{0} m^{2}_{0} E^{2}_{k}} \nonumber\\ &&
	\times \left|M_{env}\right|^{2} \left<\left|\hat{e} \cdot p_{cv}\right|^{2}\right> 
	G^{s}_{j}B_{e(g),j}\left[f^{c}_{e(g),j}+f^{v}_{e(g),j}-1\right]
\end{eqnarray}
自由電子吸収効果による屈折率変化の式は以下のようになる．
\begin{eqnarray}
  \Delta n^{w}_{fc} (t,\lambda_{k}) & = &
	- \frac
	{S(\lambda{k})\Gamma_{w}(\lambda_{k}) \hbar^{2} q^{2}}
	{2 V_{w} n_{r} \varepsilon_{0} E^{2}_{k}} 
  \left(
	\frac{ N^{c}_{W} }{ m^{*c}_{w} } f^{c}_{w} + 
	\frac{ N^{v}_{W} }{ m^{*v}_{w} } f^{v}_{w}
  \right) 
  \\
  \Delta n^{u(e,g)}_{fc} (t,\lambda_{k}) & = &
	- \frac
	{S(\lambda{k})\Gamma(\lambda_{k}) \hbar^{2} q^{2}}
	{2 V_{d} n_{r} \varepsilon_{0} E^{2}_{k}} \nonumber\\ &&
  \times
  \left( \sum_{j=1}^{2M+1} 
	  \frac{ f^{c}_{u(e,g),j} N^{c}_{U(E,G),j} }{ m^{*c}_{d} } +
	  \frac{ f^{v}_{u(e,g),j} N^{v}_{U(E,G),j} }{ m^{*v}_{d} }
  \right)
\end{eqnarray}
バンド充填効果，自由電子吸収効果による屈折率変化量の和を$\Delta n(t,E_k)$とすると位相変化量は
\begin{equation}
  \label{eq:phase_diff}
  \Delta \phi (t,E_k) = -\frac{2 \pi L_{ca}}{\lambda_{k}} \Delta n(t,E_k)
\end{equation}
と表される．

% TODO::coupling lossについて言及する
\subsection{結合部における損失}


\newpage

\section{アイダイアグラム}
図{\ref{fig:eyedia}}に信号波形とアイダイアグラムの関係性を示す．アイダイアグラムとは伝送された信号波形の多数のビットを1ビット幅の間隔で重ねたものである．アイダイアグラムを用いることによって，デジタル伝送において送信信号の信号パターンが引き起こす送信特性や受信特性の変化を観測することが可能となる．

\begin{figure}[htbp]
  \begin{center}
    \includegraphics[clip,width=6.0cm]{Eyedia.eps}
    \caption{信号波形とアイダイアグラムの関係}
    \label{fig:eyedia}
  \end{center}
\end{figure}

\newpage

\section{消光比}
アイダイアグラムを数値的に評価する指標として消光比(ER:Extinction Ratio)が用いられ，これはシステムを考える上での一つの性能指標となる．消光比(ER）は下式によって表される．
\begin{equation}
  ER(dB) = 10 \log \frac{P_{\min}^{1}}{P_{\max}^{0}}
\end{equation}
ここで，$P_{\min}^{1}$および$P_{\max}^{0}$は図{\ref{fig:extinction}}に対応しており，それぞれ出力信号光``1''に対応する最小光強度，出力信号光``0''に対応する最大光強度を表す．

\begin{figure}[htbp]
  \begin{center}
    \includegraphics[clip,width=8.0cm]{Extinction.eps}
    \caption{アイダイアグラムと消光比の関係}
    \label{fig:extinction}
  \end{center}
\end{figure}

\newpage
